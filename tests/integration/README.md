# Integration Testing

Integration tests verify API routes and database repositories.

- `access-sessions.test.ts`: Scoped session issuance and atomic code rotation session revocation.
- `content-lifecycle.test.ts`: `visible` -> `hidden` -> `deleted` lifecycle state transitions.
